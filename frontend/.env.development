# 开发环境：通过 Vite 代理访问后端，避免浏览器直连 5002 导致超时
# 请求会发往当前页面的域名（如 localhost:3008），由 dev server 转发到后端 5002
VITE_API_BASE_URL=/api

# 高德地图（用于搬迁路径「打开地图选择」精确定位）
# 注意：必须使用「Web端(JS API)」类型的 Key
VITE_AMAP_KEY=ed8aa0ce0114ec4ffe7f8c37e810955e
# 高德地图安全密钥（2021.12 后创建的 Key 必须配置）
VITE_AMAP_SECURITY_KEY=38bacc73243cc6cef9db5ff41ea956f7
